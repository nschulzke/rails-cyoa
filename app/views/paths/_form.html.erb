<%= form_with(model: path, local: true) do |form| %>
  <% if path.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(path.errors.count, "error") %> prohibited this path from being saved:</h2>

      <ul>
      <% path.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :choice %>
    <%= form.text_field :choice, id: :path_choice %>
  </div>

  <div class="field">
    <%= form.label :from_room_id %>
    <%= form.number_field :from_room_id, id: :path_from_room_id %>
  </div>

  <div class="field">
    <%= form.label :to_room_id %>
    <%= form.number_field :to_room_id, id: :path_to_room_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
